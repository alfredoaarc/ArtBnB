<div class="container">
    <h2><%=@artwork.title%></h2>
    <p><%= @artwork.description %></p>
  <div class="d-flex flex-row-reverse bd-highlight">
    <h4>Price <%=@artwork.price %>€/ Day</h4>
  </div>
  <div class="show-img-box mt-5">
    <%= cl_image_tag @artwork.photo.key, class: "artwork-img"%>
  </div>
</div>

<%= render "rentals/form", artwork: @artwork, rental: @rental %>
